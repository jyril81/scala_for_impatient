/*
4. Why do you think the Scala language designers provided
the package object syntax instead of simply letting you add
 functions and variables to a package?
  */
//JVM limitation. It requires to have class/object wrapper around it.
//Scala came up with package objects to  address ths in a nice way